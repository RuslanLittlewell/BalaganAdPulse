{
  "name": "adpulse",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "npm run dev -w apps/api",
    "test": "npm test -w apps/api",
    "build": "npm run build -w apps/api"
  }
}
