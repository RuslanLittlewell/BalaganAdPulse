node_modules
**/node_modules
dist
**/dist
# Patterns here are not recursive: a bare `.env` excludes only the one at the
# root of the build context, so `apps/api/.env` was reaching the build stage.
**/.env
**/.env.*
.git
docs
.superpowers
