## 1. Confirmations

- [x] 1.1 Add failing tests to `LeadCard.test.tsx`: Лид создан after Создать, Лид удалён after a
  confirmed delete, no success alert after a refused create. Run them and see them fail.
- [x] 1.2 Raise the alerts in `LeadCard.tsx` and add the strings; run `npm run test:web` and
  `openspec validate lead-create-delete-alerts --strict`.
